@model CovidBilgilendirme.Models.classes.CovidAbout

@{
    ViewBag.Title = "GetAboutCovid";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="row">
    <div class="col-lg-12">
        <h3 class="page-header"><i class="fa fa-laptop"></i>Covid-19 Hakkında Güncelleme</h3>
        <ol class="breadcrumb">
            <li><i class="fa fa-home"></i><a href="~/Admin/Index/">Home</a></li>
            <li><i class="fa fa-laptop"></i>Covid-19 Hakkında Güncelleme</li>
        </ol>
    </div>
</div>

@using (Html.BeginForm("UpdateAboutCovid", "Admin", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    <br />
    @Html.HiddenFor(x => x.ID)

    @Html.LabelFor(x => x.CovidTitle, new { @class = "labelCss" })
    @Html.TextBoxFor(x => x.CovidTitle, new { @class = "form-control black"})
    <br />
    @Html.LabelFor(x => x.CovidDescription, new { @class = "labelCss" })
    @Html.TextAreaFor(x => x.CovidDescription, 4, 1, new { @class = "form-control black"})
    <br />
    @Html.LabelFor(x => x.CovidImageUrl, new { @class = "imageLabel"})
    @Html.TextBoxFor(x => x.CovidImageUrl, new { @class = "form-group", type = "file" })
    <button class="btn btn-success textBold">Hakkımızda Güncelle</button>
}


